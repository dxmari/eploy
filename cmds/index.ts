import deploy from './deploy'
import help from './help'
import { run, start, restart, stop, status, deleteService, list } from './manage-server'
import transfer from './transfer'
import version from './version'
export {
    help, version, run, start, restart, list, stop, status, deleteService, deploy, transfer
}